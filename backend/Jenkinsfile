pipeline {
    agent any

    stages {
        stage('Hello Jenkins') {
            steps {
                echo 'Hello Jenkins'
            }
        }
    }

    post {
        always {
            echo 'Pipeline finished.'
        }
    }
}
