pipeline {
    agent any
    stages {
        stage('Verify Docker Access') {
            steps {
                script {
                    sh 'docker --version'
                }
            }
        }

        // Rest of the stages remain the same...
    }
}
